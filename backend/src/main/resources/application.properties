# Spring Boot service name (for logs/metadata).
spring.application.name=taskmanager

# Local MongoDB connection string (database: taskmanager_db).
spring.data.mongodb.uri=mongodb://localhost:27017/taskmanager_db

# API server port.
server.port=8080
